## 🛠 Ejercicios Prácticos

1.  **Cálculo de Área:** Crea un archivo PHP. Define dos variables, `$base` y `$altura`, con valores numéricos. Calcula el área de un triángulo (`(base * altura) / 2`). Usa `echo` para mostrar el resultado final con un mensaje descriptivo.
2.  **Par o Impar:** Define una variable `$numero`. Usa el operador **Módulo (`%`)** y una sentencia `if/else` para determinar si el número es **par** (`$numero % 2 == 0`) o **impar**, y muestra el resultado apropiado.
3.  **Saludo Dinámico por Hora:** Usa la función `date("H")` para obtener la hora actual. Implementa un bloque `if/else` que muestre:
      * "Buenos días" si la hora es menor a 12.
      * "Buenas tardes" si la hora es igual o mayor a 12.